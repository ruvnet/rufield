[package]
name = "rufield-adapters"
version.workspace = true
edition.workspace = true
description = "RuField adapters for deterministic simulation, captured WiFi CSI replay, and calibrated quantum RF vector replay"
license.workspace = true
authors.workspace = true
repository.workspace = true
keywords = ["sensing", "simulator", "csi", "radar", "quantum"]
categories = ["science", "simulation"]

[dependencies]
rufield-core = { workspace = true }
rufield-provenance = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
rufield-provenance = { workspace = true }
rufield-fusion = { workspace = true }

[lints]
workspace = true
